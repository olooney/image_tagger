[project]

name = "image-tagger"
version = "1.0.0"
description = "Tag, categorize, and clean up images with vision models."
readme = "README.md"
requires-python = ">=3.14"

dependencies = [
    "jinja2>=3.1.6",
    "matplotlib>=3.10.9",
    "ollama>=0.6.2",
    "openai>=2.41.0",
    "pandas>=3.0.3",
    "pillow>=12.2.0",
    "pillow-avif-plugin>=1.5.5",
    "pillow-heif>=1.1.1",
    "pre-commit>=4.6.0",
    "pytest>=9.0.2",
    "pydantic>=2.13.4",
    "fastapi>=0.116.0",
    "httpx2>=0.1.0",
    "python-multipart>=0.0.20",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "send2trash>=1.8.3",
    "tenacity>=9.1.4",
    "torch>=2.9.1",
    "transformers>=4.57.3",
    "ty>=0.0.44",
    "uvicorn>=0.35.0",
    "pytest-cov>=7.1.0",
    "opencv-python-headless>=5.0.0.93",
]

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.setuptools]
py-modules = ["cli", "constants", "convert", "image_tagger", "review_app", "stackmap", "transform", "util"]
packages = ["image_tagger_data"]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.package-data]
image_tagger_data = ["*"]

[tool.ruff.lint]
select = [
    "E",   # PEP 8
    "F",   # Pyflakes
    "I",   # import sort
    "B",   # bugbear
    "UP",  # PyUpgrade
    "TRY", # Tryceratops
]
ignore = [
    "E501",   # line length
    "TRY003", # long exception messages
    "TRY300", # return outside of else
    "B905",   # explicit strict in zip()
]
